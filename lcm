package programs;
public class lcm {
	public static void main(String[] args) {
		int a=5, b=10, lcm=0;
		int max = (a > b) ? a:b;
		for(int i=max; i<a*b; i++) {
			if(i%a==0 && i%b==0) lcm =i;
		}
		System.out.print(lcm);
	}
}
